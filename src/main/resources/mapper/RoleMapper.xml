<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lcx.campus.mapper.RoleMapper">

    <select id="selectRoleByUserId" resultType="com.lcx.campus.domain.Role" parameterType="java.lang.Long">
        select distinct r.*
        from sys_role r
                 left join sys_user_role ur on r.role_id = ur.role_id
        where ur.user_id = #{userId}
    </select>
    <select id="isAdminByUserId" resultType="java.lang.Boolean">
        select count(1) > 0
        from sys_role r
                 left join sys_user_role ur on r.role_id = ur.role_id
        where ur.user_id = #{userId} and r.role_key = 'admin'
    </select>
</mapper>
